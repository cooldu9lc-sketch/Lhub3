# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def goodNodes(self, root: TreeNode) -> int:
        
        def recur(node,maxi=float("-inf")):
            if not node:return 0
            return int(node.val>=maxi) and recur(node.left,max(maxi,node.val)) + recur(node.right+max(maxi,node.val))
        return recur(root)

  ### THIS CODE DOES NOT WORK AND GIVES RUN TIME ERROR
